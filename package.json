{
  "name": "bump-version",
  "version": "1.0.0",
  "description": "GitHub Action for automated version bump for internal monorepo",
  "main": "index.js",
  "scripts": {
    "test": "npx jest",
    "format:write": "npx prettier --write .",
    "format:check": "npx prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "jest": {
    "verbose": true,
    "clearMocks": true,
    "testEnvironment": "node",
    "moduleFileExtensions": [
      "js"
    ],
    "testMatch": [
      "**/*.test.js"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prettier": "^3.2.5"
  }
}
